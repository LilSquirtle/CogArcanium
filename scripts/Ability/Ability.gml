///Ability(AbilityName, Character, Target)

//var name = argument0;
//var character = argument1;
//var target = argument2;

//if(character.curMana < ds_map_find_value(global.AbilityCost, name)){ return false; }
//else{ script_execute(name, character, target); return true; }
